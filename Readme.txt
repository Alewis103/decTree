This decision tree project takes as input height,weight, and shoe size as input (it is hardcoded in at the moment) 
and returns a value of male or female . The model is only trained on 11 different lists of data and can be increased to more.
